package main

/*	a data type is a way to specify
	how data should be interpreted
	go uses static typing , which is checked at compile time
	-the compiler uses type interference which automatically
	detremines which types to use .
	Type aliases can be created to give new names to exisiting
	types .
	type UserID int
	Converting between types requires parentheses
	UserID(5)
*/
